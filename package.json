{
  "name": "serverless-orders-crud",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "jest --coverage",
    "test:watch": "jest --watch",
    "local:start": "docker-compose up -d",
    "local:stop": "docker-compose down",
    "local:setup": "bash ./scripts/setup-local.sh",
    "local:deploy": "bash ./scripts/package-and-create-lambdas.sh",
    "local:invoke:generate": "bash ./scripts/generate-invoke-commands.sh",
    "local:invoke": "bash ./scripts/invoke-lambdas.sh"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.400.0",
    "@aws-sdk/lib-dynamodb": "^3.400.0",
    "aws-lambda": "^1.0.7",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.157",
    "@types/jest": "^29.5.0",
    "@types/uuid": "^10.0.0",
    "aws-sdk-client-mock": "^2.2.0",
    "cross-env": "^7.0.3",
    "jest": "^29.6.0",
    "ts-jest": "^29.1.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.0"
  }
}
